# Delivery System Plan

Phase 1 prepares the project for delivery fees without changing checkout behavior yet.

Initial assumptions:

- Default delivery fee is NGN 3,000.
- Future pricing should support country, state, and local region matching.
- Delivery status should eventually be tracked separately from order status.
- Promo codes are out of scope until inventory and delivery are stable.
